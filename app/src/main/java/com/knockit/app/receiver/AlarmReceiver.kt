package com.knockit.app.receiver

import android.content.BroadcastReceiver
import android.content.Context
import android.content.Intent

/**
 * Stub — full implementation by the services agent.
 * Receives alarm intents and fires the corresponding notification.
 */
class AlarmReceiver : BroadcastReceiver() {
    override fun onReceive(context: Context, intent: Intent) {
        // TODO: services agent will implement notification firing here
    }
}
